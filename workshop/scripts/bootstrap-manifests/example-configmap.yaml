apiVersion: v1
kind: ConfigMap
metadata:
  name: ${USERNAME}-bootstrap-example
  namespace: ${NAMESPACE}
  labels:
    app.kubernetes.io/managed-by: workshop-bootstrap
data:
  username: ${USERNAME}
  namespace: ${NAMESPACE}
