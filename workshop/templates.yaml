apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: workshop-templates
  description: |
    A collection of workshop templates
spec:
  type: url
  targets:
    - ./developer-experience/gitea/template.yaml
    - ./developer-experience/gitlab/template.yaml
    - ./platform/groups.yaml
    - ./platform/workshop-collection.yaml
